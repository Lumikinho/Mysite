---
const categories = {
  "Front-end": ["HTML5", "CSS3", "TailwindCSS"],
  "Back End": ["JavaScript", "MySql", "NodeJs"],
  "Frameworks": ["Express.js", "MySql2"],
  "Ferramentas": ["Git", "VS Code", "insomnia"],
};
---

<div class="rounded-2xl border border-zinc-800 bg-zinc-900/40 p-6 flex flex-col gap-4">
  <h2 class="text-xl font-semibold">Habilidades</h2>
  {Object.entries(categories).map(([category, skills]) => (
    <div class="flex flex-col gap-2">
      <h3 class="text-lg font-semibold text-zinc-300">{category}:</h3>
      <div class="flex flex-wrap gap-2">
        {skills.map(skill => (
          <span class="text-sm font-medium bg-zinc-800 text-zinc-300 px-3 py-1 rounded-full">{skill}</span>
        ))}
      </div>
    </div>
  ))}
</div>
